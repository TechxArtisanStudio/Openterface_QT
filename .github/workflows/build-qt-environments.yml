name: Build Qt Environments (Static or Shared)

on:
  workflow_dispatch:
    inputs:
      environment_type:
        description: 'Type of environment to build'
        required: true
        default: 'shared'
        type: choice
        options:
          - 'static'
          - 'shared'
      step:
        description: 'Which step to build'
        required: false
        default: 'all'
        type: choice
        options:
          - 'setup'
          - 'ffmpeg'
          - 'gstreamer'
          - 'qt-base-qtml'
          - 'qt-declarative'
          - 'qt-complete'
          - 'all'
      ubuntu_version:
        description: 'Ubuntu base image version'
        required: false
        default: '22.04'
        type: choice
        options:
          - '22.04'
          - '24.04'
      force_rebuild:
        description: 'Force rebuild even if image exists'
        required: false
        default: false
        type: boolean
      push_to_registry:
        description: 'Push to container registry'
        required: false
        default: true
        type: boolean
  push:
    branches: [main]
    paths:
      - 'docker/**'
      - '.github/workflows/build-qt-environments.yml'
  pull_request:
    branches: [main]
    paths:
      - 'docker/**'
      - '.github/workflows/build-qt-environments.yml'

env:
  REGISTRY: ghcr.io

jobs:
  # Static Qt Environment Build (existing multi-stage process)
  build-setup:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.step == 'setup' || github.event.inputs.step == 'ffmpeg' || github.event.inputs.step == 'gstreamer' || github.event.inputs.step == 'qt-base-qtml' || github.event.inputs.step == 'qt-declarative' || github.event.inputs.step == 'qt-complete' || github.event.inputs.step == 'all') || github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      base-digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Shared or Static build in env
        run: |
          if [ "${{ github.event.inputs.environment_type || 'static' }}" = "shared" ]; then
            echo "SHARED_OR_STATIC=shared" >> $GITHUB_ENV
          else
            echo "SHARED_OR_STATIC=static" >> $GITHUB_ENV
          fi

      - name: Set lowercase image name
        env:
          OWNER: ${{ github.repository_owner }}
          SHARED: ${{ env.SHARED_OR_STATIC }}
        run: |
          OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=${OWNER}/${SHARED}-qtbuild" >> $GITHUB_ENV

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-setup
          tags: |
            type=ref,event=branch,suffix=-amd64
            type=ref,event=pr,suffix=-amd64
            type=sha,suffix=-amd64
            type=raw,value=latest-amd64
            type=raw,value=ubuntu-${{ github.event.inputs.ubuntu_version || '22.04' }}-amd64

      - name: Check if setup image exists
        id: check-setup
        if: github.event_name != 'pull_request'
        run: |
          if [[ "${{ github.event.inputs.force_rebuild }}" == "true" ]]; then
            echo "Force rebuild requested, will build base image"
            echo "should_build=true" >> $GITHUB_OUTPUT
          else
            IMAGE_TAG="ubuntu-${{ github.event.inputs.ubuntu_version || '22.04' }}-amd64"
            echo "Checking for image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-setup:$IMAGE_TAG"
            if docker manifest inspect ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-setup:$IMAGE_TAG > /dev/null 2>&1; then
              echo "Base image already exists for architecture amd64, skipping build"
              echo "should_build=false" >> $GITHUB_OUTPUT
            else
              echo "Base image does not exist for architecture amd64, will build"
              echo "should_build=true" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Build and push Setup image
        id: build
        if: steps.check-setup.outputs.should_build == 'true' || github.event_name == 'push' || github.event_name == 'pull_request'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.qt-setup
          build-args: |
            UBUNTU_VERSION=${{ github.event.inputs.ubuntu_version || '24.04' }}
            TARGETARCH=amd64
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_to_registry == 'true' || github.event.inputs.push_to_registry == '') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ env.SHARED_OR_STATIC }}-setup
          cache-to: type=gha,mode=max,scope=${{ env.SHARED_OR_STATIC }}-setup
          platforms: linux/amd64

  build-ffmpeg:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.step == 'ffmpeg' || github.event.inputs.step == 'gstreamer' || github.event.inputs.step == 'qt-base-qtml' || github.event.inputs.step == 'qt-declarative' || github.event.inputs.step == 'qt-complete' || github.event.inputs.step == 'all') || github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    needs: [build-setup]
    permissions:
      contents: read
      packages: write
    outputs:
      ffmpeg-digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Shared or Static build in env
        run: |
          if [ "${{ github.event.inputs.environment_type || 'static' }}" = "shared" ]; then
            echo "SHARED_OR_STATIC=shared" >> $GITHUB_ENV
          else
            echo "SHARED_OR_STATIC=static" >> $GITHUB_ENV
          fi

      - name: Set lowercase image name
        env:
          OWNER: ${{ github.repository_owner }}
          SHARED: ${{ env.SHARED_OR_STATIC }}
        run: |
          OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=${OWNER}/${SHARED}-qtbuild" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-ffmpeg
          tags: |
            type=ref,event=branch,suffix=-amd64
            type=ref,event=pr,suffix=-amd64
            type=sha,suffix=-amd64
            type=raw,value=latest-amd64
            type=raw,value=ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64

      - name: Build and push FFmpeg image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.qt-ffmpeg
          build-args: |
            BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-setup:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64
            UBUNTU_VERSION=${{ github.event.inputs.ubuntu_version || '24.04' }}
            TARGETARCH=amd64
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_to_registry == 'true' || github.event.inputs.push_to_registry == '') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ env.SHARED_OR_STATIC }}-ffmpeg
          cache-to: type=gha,mode=max,scope=${{ env.SHARED_OR_STATIC }}-ffmpeg
          platforms: linux/amd64

  build-qt-base-qtml:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.step == 'qt-base-qtml' || github.event.inputs.step == 'qt-declarative' || github.event.inputs.step == 'qt-complete' || github.event.inputs.step == 'all') || github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    needs: [build-gstreamer]
    permissions:
      contents: read
      packages: write
    outputs:
      qt-base-qtml-digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Shared or Static build in env
        run: |
          if [ "${{ github.event.inputs.environment_type || 'static' }}" = "shared" ]; then
            echo "SHARED_OR_STATIC=shared" >> $GITHUB_ENV
          else
            echo "SHARED_OR_STATIC=static" >> $GITHUB_ENV
          fi

      - name: Set lowercase image name
        env:
          OWNER: ${{ github.repository_owner }}
          SHARED: ${{ env.SHARED_OR_STATIC }}
        run: |
          OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=${OWNER}/${SHARED}-qtbuild" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-qt-base-qtml
          tags: |
            type=ref,event=branch,suffix=-amd64
            type=ref,event=pr,suffix=-amd64
            type=sha,suffix=-amd64
            type=raw,value=latest-amd64
            type=raw,value=ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64

      - name: Build and push Qt Base QTML image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.qt-base-qtml
          build-args: |
            BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-gstreamer:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64
            UBUNTU_VERSION=${{ github.event.inputs.ubuntu_version || '24.04' }}
            TARGETARCH=amd64
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_to_registry == 'true' || github.event.inputs.push_to_registry == '') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ env.SHARED_OR_STATIC }}-qt-base-qtml
          cache-to: type=gha,mode=max,scope=${{ env.SHARED_OR_STATIC }}-qt-base-qtml
          platforms: linux/amd64

  build-gstreamer:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.step == 'gstreamer' || github.event.inputs.step == 'qt-base-qtml' || github.event.inputs.step == 'qt-declarative' || github.event.inputs.step == 'qt-complete' || github.event.inputs.step == 'all') || github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    needs: [build-qt-base-qtml]
    permissions:
      contents: read
      packages: write
    outputs:
      gstreamer-digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Shared or Static build in env
        run: |
          if [ "${{ github.event.inputs.environment_type || 'static' }}" = "shared" ]; then
            echo "SHARED_OR_STATIC=shared" >> $GITHUB_ENV
          else
            echo "SHARED_OR_STATIC=static" >> $GITHUB_ENV
          fi

      - name: Set lowercase image name
        env:
          OWNER: ${{ github.repository_owner }}
          SHARED: ${{ env.SHARED_OR_STATIC }}
        run: |
          OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=${OWNER}/${SHARED}-qtbuild" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-gstreamer
          tags: |
            type=ref,event=branch,suffix=-amd64
            type=ref,event=pr,suffix=-amd64
            type=sha,suffix=-amd64
            type=raw,value=latest-amd64
            type=raw,value=ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64

      - name: Build and push GStreamer image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.qt-gstreamer
          build-args: |
            BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-ffmpeg:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64
            UBUNTU_VERSION=${{ github.event.inputs.ubuntu_version || '24.04' }}
            TARGETARCH=amd64
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_to_registry == 'true' || github.event.inputs.push_to_registry == '') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ env.SHARED_OR_STATIC }}-gstreamer
          cache-to: type=gha,mode=max,scope=${{ env.SHARED_OR_STATIC }}-gstreamer
          platforms: linux/amd64

  build-qt-declarative:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.step == 'qt-declarative' || github.event.inputs.step == 'qt-complete' || github.event.inputs.step == 'all') || github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    needs: [build-gstreamer]
    permissions:
      contents: read
      packages: write
    outputs:
      qt-declarative-digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Shared or Static build in env
        run: |
          if [ "${{ github.event.inputs.environment_type || 'static' }}" = "shared" ]; then
            echo "SHARED_OR_STATIC=shared" >> $GITHUB_ENV
          else
            echo "SHARED_OR_STATIC=static" >> $GITHUB_ENV
          fi

      - name: Set lowercase image name
        env:
          OWNER: ${{ github.repository_owner }}
          SHARED: ${{ env.SHARED_OR_STATIC }}
        run: |
          OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=${OWNER}/${SHARED}-qtbuild" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-qt-declarative
          tags: |
            type=ref,event=branch,suffix=-amd64
            type=ref,event=pr,suffix=-amd64
            type=sha,suffix=-amd64
            type=raw,value=latest-amd64
            type=raw,value=ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64

      - name: Build and push Qt Declarative image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.qt-declarative
          build-args: |
            BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-qt-base-qtml:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64
            UBUNTU_VERSION=${{ github.event.inputs.ubuntu_version || '24.04' }}
            TARGETARCH=amd64
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_to_registry == 'true' || github.event.inputs.push_to_registry == '') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ env.SHARED_OR_STATIC }}-qt-declarative
          cache-to: type=gha,mode=max,scope=${{ env.SHARED_OR_STATIC }}-qt-declarative
          platforms: linux/amd64

  build-complete:
    if: ${{ github.event_name == 'workflow_dispatch' && (github.event.inputs.step == 'qt-complete' || github.event.inputs.step == 'all') || github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    needs: [build-qt-declarative]
    permissions:
      contents: read
      packages: write
    outputs:
      qt-digest: ${{ steps.build.outputs.digest }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set Shared or Static build in env
        run: |
          if [ "${{ github.event.inputs.environment_type || 'static' }}" = "shared" ]; then
            echo "SHARED_OR_STATIC=shared" >> $GITHUB_ENV
          else
            echo "SHARED_OR_STATIC=static" >> $GITHUB_ENV
          fi

      - name: Set lowercase image name
        env:
          OWNER: ${{ github.repository_owner }}
          SHARED: ${{ env.SHARED_OR_STATIC }}
        run: |
          OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=${OWNER}/${SHARED}-qtbuild" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-complete
          tags: |
            type=ref,event=branch,suffix=-amd64
            type=ref,event=pr,suffix=-amd64
            type=sha,suffix=-amd64
            type=raw,value=latest-amd64
            type=raw,value=ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64

      - name: Build and push Qt Complete image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.qt-complete
          build-args: |
            BASE_IMAGE=${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-qt-declarative:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64
            UBUNTU_VERSION=${{ github.event.inputs.ubuntu_version || '24.04' }}
            TARGETARCH=amd64
          push: ${{ github.event_name != 'pull_request' && (github.event.inputs.push_to_registry == 'true' || github.event.inputs.push_to_registry == '') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ env.SHARED_OR_STATIC }}-qt-complete
          cache-to: type=gha,mode=max,scope=${{ env.SHARED_OR_STATIC }}-qt-complete
          platforms: linux/amd64

      - name: Test Qt Environment
        run: |
          docker run --rm ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-complete:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64 \
            /opt/Qt6/bin/verify-qt-installation.sh

  # Summary job that shows which images are available
  build-summary:
    runs-on: ubuntu-latest
    needs: [build-complete]
    if: always()
    steps:
      - name: Set Shared or Static build in env
        run: |
          if [ "${{ github.event.inputs.environment_type || 'static' }}" = "shared" ]; then
            echo "SHARED_OR_STATIC=shared" >> $GITHUB_ENV
          else
            echo "SHARED_OR_STATIC=static" >> $GITHUB_ENV
          fi
          
      - name: Set lowercase image name
        env:
          OWNER: ${{ github.repository_owner }}
          SHARED: ${{ env.SHARED_OR_STATIC }}
        run: |
          OWNER=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')
          echo "IMAGE_NAME_LOWER=${OWNER}/${SHARED}-qtbuild" >> $GITHUB_ENV

      - name: Build Summary
        run: |
          echo "## Qt Build Environment Summary" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Environment | Type | Status | Image |" >> $GITHUB_STEP_SUMMARY
            echo "|-------------|------|--------|-------|" >> $GITHUB_STEP_SUMMARY

            # Qt Complete (static) — reflect job result
            if [ "${{ needs.build-complete.result }}" = "success" ]; then
            echo "| Qt Complete | static | ✅ Success | \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-complete:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64\` |" >> $GITHUB_STEP_SUMMARY
            else
            echo "| Qt Complete | static | ❌ Failed/Skipped | - |" >> $GITHUB_STEP_SUMMARY
            fi

            # Qt Shared — check if shared image is present in registry
            IMAGE_TAG="ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64"
            if docker manifest inspect ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-shared:$IMAGE_TAG > /dev/null 2>&1; then
            echo "| Qt Shared | shared | ✅ Available | \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-shared:$IMAGE_TAG\` |" >> $GITHUB_STEP_SUMMARY
            else
            echo "| Qt Shared | shared | ❌ Not available | - |" >> $GITHUB_STEP_SUMMARY
            fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Usage Instructions" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Shared Build:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker run --rm -v \$(pwd):/workspace/src -w /workspace/src \\" >> $GITHUB_STEP_SUMMARY
          echo "  ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-shared:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64 \\" >> $GITHUB_STEP_SUMMARY
          echo "  bash -c 'mkdir build && cd build && cmake -DBUILD_SHARED_LIBS=ON .. && make -j\$(nproc)'" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Static Build:**" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
          echo "docker run --rm -v \$(pwd):/workspace/src -w /workspace/src \\" >> $GITHUB_STEP_SUMMARY
          echo "  ${{ env.REGISTRY }}/${{ env.IMAGE_NAME_LOWER }}-complete:ubuntu-${{ github.event.inputs.ubuntu_version || '24.04' }}-amd64 \\" >> $GITHUB_STEP_SUMMARY
          echo "  bash -c 'mkdir build && cd build && cmake -DCMAKE_PREFIX_PATH=/opt/Qt6 -DBUILD_SHARED_LIBS=OFF .. && make -j\$(nproc)'" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
